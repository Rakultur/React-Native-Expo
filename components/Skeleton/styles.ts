import { StyleSheet } from 'react-native';

export const useStyles = () => StyleSheet.create({
    container: {
        padding: 10,
        backgroundColor: '#eee',
        borderRadius: 8,
    },
});
