import { StyleSheet } from "react-native";

export const createThemeSwitcherStyles = (theme: any) =>
    StyleSheet.create({
        container: {
            flexDirection: "row",
            flexWrap: "wrap",
            justifyContent: "center",
            alignItems: "center",
        },

        button: {
        },

        activeButton: {
        },
    });